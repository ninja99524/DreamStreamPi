import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vercel.napapp.dreamstream',
  appName: 'DreamStream',
  webDir: 'dist'
};

export default config;
